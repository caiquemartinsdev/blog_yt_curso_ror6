<%= simple_form_for article do |form| %>
  <%= form.input :title %>
  <%= form.input :body, input_html: { rows: 10 }%>
  <%= form.association :category, collection: Category.sorted, prompt: "Choose a Category"%>
  <br>

  <p>
      <%= form.button :submit, class: 'btn btn-primary' %>
      <%= link_to 'Back', root_path, class: 'btn btn-secondary' %>
  </p>
<% end %>
<!--
HTML ERB COMENTADO AO INVÉS DE SER EXCLUÍDO POIS PODERÁ SERVIR PARA
UMA POSSÍVEL CONSULTA

<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
-->